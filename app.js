window.onload=function(){
    /*
    var d=document;
    d.id=d.getElementById;
    var canvas=d.id('canvas');
    var c=canvas.getContext('2d');
    c.fillStyle='#FF0000';
    c.fillRect(0,0,80,100);
    */
    _(1).get
}